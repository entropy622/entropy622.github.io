---
export const prerender = true
export function getStaticPaths() {
  return Array.from({ length: 10 }, (_, index) => ({ params: { n: String(index + 1) } }))
}
return Astro.redirect('/blog', 301)
---
